import { describe, expect, it } from 'vitest';

// Smoke test placeholder so CI has at least one test suite.

describe('core', () => {
  it('vitest runs', () => {
    expect(1 + 1).toBe(2);
  });
});
