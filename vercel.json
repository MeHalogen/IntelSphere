{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run -w @crisis-monitor/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
